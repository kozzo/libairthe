<div class="card travel-card">

    <div class="card-body">
        <h5 class="card-title">{{ child.label }}</h5>
    </div>

    <div class="card-footer d-flex m-auto">
        <a href="{{ path('app_category_travels', {'id': child.id}) }}" class="btn btn-primary">Voir les voyages</a>
    </div>
</div>
