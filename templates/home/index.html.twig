{% extends 'base.html.twig' %}

{% block content %}
	{% include 'components/search_bar.html.twig' %}

	<div style="height: 600px; width: 100%">
		<h2 class="text-center mt-4">
			<img src="{{ asset('images/icons/nouveau.png') }}"
				 alt="Nouveau"
				 aria-label="Nouveau"
				 style="width: 50px; height: 50px;">
			Derniers Voyages Ajoutés
			<img src="{{ asset('images/icons/nouveau.png') }}"
				 alt="Nouveau"
				 aria-label="Nouveau"
				 style="width: 50px; height: 50px;">
		</h2>
		<div class="container mt-4">
			<div class="row">
				{% for child in latestTravels %}
					<div class="col mb-4">
						{{ include('travel/_partials/travel-card.html.twig') }}
					</div>
				{% endfor %}
			</div>
		</div>
	</div>
{% endblock %}
