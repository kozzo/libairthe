<form method="post" action="{{ path('app_user_crud_delete', {'slug': user.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.slug) }}">
    <button class="btn">Delete</button>
</form>
